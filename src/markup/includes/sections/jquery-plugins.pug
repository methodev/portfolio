mixin jquery(data)
  ul.items
    each item in data.content
      li.item
        header.bullet.cf
          h3.name
            a(href='http://' + item.name + '.martinmetodiev.com', target='_blank') #{item.title}
          span.info #{item.description}
          span.period.meta.light #{item.date}
