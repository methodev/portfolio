mixin react(data)
  ul.items
    each item in data.content
      li.item
        header.bullet.cf
          h3.name
            a(href='https://github.com/martinmethod/' + item.name, target='_blank') #{item.title}
          span.info #{item.description}
          span.period.meta.light #{item.date}
